public class Parameters {
    public static void main(String[] args) {
        measureRectangle(4.2, 2.3);
        measureRectangle(1.2, 1.3);
        measureRectangle(2.2, 4.3);
        measureRectangle(3.2, 5.3);
        measureRectangle(5.2, 3.3);
        measureRectangle(6.2, 6.3);
        


      

       
   
       }
    
   
       public static void measureRectangle(double length , double width){
        double area = length * width;
        System.out.println("the area of the rectangle is " + area);

        System.out.println("the area of the rectangle with l " + length + "  and w " + width + " is " + area);
       }
           
        // parameters are value func expects to receve
        // arguments are .. that we are passing 

    
       }
